#include<stdio.h> 

int main() {
	int a, b, SUM, SUBTRACTION, PRODUCT, QUOTIENT;
	a = 24;
	b = 8;
	SUM = a + b;
	SUBTRACTION = a - b;
	PRODUCT = a * b;
	QUOTIENT = a / b;
	printf("SUM cua a va b : %d\n",SUM );
	printf("SUBTRACTIO cua a va b : %d\n",SUBTRACTION);
	printf("PRODUCT cua a va b : %d\n",PRODUCT);
	printf("QUOTIENT cua a va b : %d",QUOTIENT);
}
